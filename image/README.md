# Local Images

Generated plots remain local and are intentionally ignored by Git. Recreate
them from a saved local pickle record with the project's visualization entry
point; the record metadata is listed in `archive_manifest.json`.
