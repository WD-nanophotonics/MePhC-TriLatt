# Local Data

This directory stores local MPB pickle records and `_tmp` working records.
Binary records are intentionally ignored by Git. Their task parameters,
compute parameters, timestamps, and hashes are tracked in the root
`archive_manifest.json` when a workflow updates it.
