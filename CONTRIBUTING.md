# Contributing To TriLatt

Keep geometry and physical parameters in `config.py`. Keep resolution, grid,
Berry step, EFS settings, and plot settings in their task scripts. Do not add
local `.pkl` records or generated images to Git.

Changes to shared geometry, k-space conventions, or record schemas belong in
MePhC and should identify the MePhC tag or commit they require.
